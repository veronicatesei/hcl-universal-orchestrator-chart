# *******************************************************************************
# Licensed Materials - Property of HCL
# (c) Copyright HCL Technologies Ltd. 2024. All Rights Reserved.
# 
# Note to U.S. Government Users Restricted Rights:
# Use, duplication or disclosure restricted by GSA ADP Schedule
# *******************************************************************************
{{ $fullname := include "agenticbuilder.fullname" . }}
{{- $root := . -}}

apiVersion: v1
kind: Service
metadata:
  name: "{{ $fullname }}-{{ .Values.credentialManager.name }}"
  labels:
{{ include "agenticbuilder.selectorLabels.backend" (list $root .Values.credentialManager) | indent 4 }}
spec:
  selector: {{- include "agenticbuilder.selectorLabels.backend" (list $root .Values.credentialManager) | nindent 4 }}
  ports:
    - name: port-{{ default 8080 .Values.credentialManager.port }}
      protocol: TCP
      port: {{ default 8080 .Values.credentialManager.port }}
      targetPort: port-{{ default 8080 .Values.credentialManager.port }}
  type: ClusterIP